syntax = "proto3";
package pb;
option go_package = "./;pb";

// definição de contrato
message User {
    string id = 1;
    string name = 2;
    string email = 3;
}

// definição do rpc (server function)
service UserService {
    rpc AddUser(User) returns (User);
}